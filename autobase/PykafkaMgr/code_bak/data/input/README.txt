
hi, welcome ~

produceMsg.json
=================================
输入json格式数据，作为生产者消息的输入。
1.支持多条json数据输入。格式如下：
	[
	json1,
	json2,
	...,
	jsonN
	]
总体结构是：[  ,  ]

2.此json文件不能加注释，因为会破坏json文件格式，导致无法解析
3.输入只要是json格式，不需要关注是不是一行或多行，多换行、空格等都不影响解析